name: merge-bot-retired

# ADR-0002 : le propriétaire fusionne. Aucun bot ne fusionne une PR CityLab.
on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Merge bot retiré (ADR-0002) ; aucune auto-fusion."
