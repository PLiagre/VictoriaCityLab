name: pipeline-verify-retired

# ADR-0002 : plus d'archivage auto-fusionné, plus de pull_request_target
# vers un runner personnel sur un dépôt public.
on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - run: echo "pipeline-verify retiré (ADR-0002)."
