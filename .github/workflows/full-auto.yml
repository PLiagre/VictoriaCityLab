name: citylab-full-auto-retired

# ADR-0002 : plus de production ni de fusion automatique. Le fichier reste
# déclenchable pour rendre le retrait visible et réversible.
on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - run: |
          echo "Pipeline full-auto retiré (ADR-0002)."
          echo "Hermes propose ; Claude écrit le brief ; Cursor exécute ;"
          echo "le propriétaire fusionne. Aucun cron ne produit."
          exit 0
