# Rapports Asset Factory

`source_inventory.json` est généré sans Unity par :

```powershell
py Tools/AssetFactory/citylab_factory.py scan
```

Le mode `--check` vérifie que le catalogue versionné correspond toujours aux
sources présentes.
