# City Mode minimal host

This Unity project intentionally contains no CityLab scene, fixture, simulation,
save service, or host asset. Its manifest imports only the versioned contracts,
the production presentation lifecycle, and Unity Test Framework.
